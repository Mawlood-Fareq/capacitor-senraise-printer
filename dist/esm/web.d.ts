import { WebPlugin } from '@capacitor/core';
import type { PrinterPlugin } from './definitions';
export declare class PrinterWeb extends WebPlugin implements PrinterPlugin {
    printEpson(options: {
        data: number[];
    }): Promise<void>;
    printText(options: {
        text: string;
    }): Promise<void>;
    printBarCode(options: {
        data: string;
        symbology: number;
        height: number;
        width: number;
    }): Promise<void>;
    printQRCode(options: {
        data: string;
        modulesize: number;
        errorlevel: number;
    }): Promise<void>;
    setAlignment(options: {
        alignment: number;
    }): Promise<void>;
    setTextSize(options: {
        textSize: number;
    }): Promise<void>;
    nextLine(options: {
        line: number;
    }): Promise<void>;
    printTableText(options: {
        text: string[];
        weight: number[];
        alignment: number[];
    }): Promise<void>;
    setTextBold(options: {
        bold: boolean;
    }): Promise<void>;
    setDark(options: {
        value: number;
    }): Promise<void>;
    setLineHeight(options: {
        lineHeight: number;
    }): Promise<void>;
    setTextDoubleWidth(options: {
        enable: boolean;
    }): Promise<void>;
    setTextDoubleHeight(options: {
        enable: boolean;
    }): Promise<void>;
    printBASE64PNG(options: {
        pic: string;
    }): Promise<void>;
    test(): Promise<void>;
}
//# sourceMappingURL=web.d.ts.map